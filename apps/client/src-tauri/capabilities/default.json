{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs plus our custom server commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
