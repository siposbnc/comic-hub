{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the reader window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
